<!DOCTYPE html>
<html lang="en">

    <%- include('./partials/head') %> 

<body>
    <%- include('./partials/header') %> 

    <main>
        <div class="user-detail container">
            <section class="top main-content">
               
                <img class="profile" src="<%=data.usuarios[0].foto %>" alt="">
                <div class="info-usuario">
                    <p><%=data.usuarios[0].username%></p> 
                    <a class="follow" href="/users/editarPerfil"><i class="fa-solid fa-user-gear"></i></a>
                    <ul class="meta">
                        <li><%=data.usuarios[0].posteos %> posteos </li>
                        <li><%= data.usuarios[0].seguidores %> seguidores</li>
                        <li><%= data.usuarios[0].seguidos %> seguidos</li>
                    </ul>
                    
                </div>
            </section>
            <section class="bottom">
                <% for (let i=0; i<data.posteos.length; i++){  %> 
                <article class="post">
                    <% let url= `/posts/detallePost/${i}`%> 
                    <a href="<%= url%>">
                        <img src="<%=data.posteos[i].foto %>" alt="">
                        <p><%=data.posteos[i].texto%></p>
                    </a>
                </article>
           <% } %>
            </section>
        </div>
    </main>

    <%- include('./partials/footer') %> 
</body>

</html>